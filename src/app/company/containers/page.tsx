import styles from "../../page.module.css";

export default function CompanyContainersPage() {
    return (
        <div className={styles.pageContainer}>
            <h1 className={styles.pageTitle}>Контейнеры</h1>
            <p className={styles.pageText}>Информация о контейнерах</p>
        </div>
    );
}
